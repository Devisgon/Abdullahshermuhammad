import { ArrowUpRight, ArrowRight, Phone, MessageSquare, Check, Workflow } from 'lucide-react';
import type { OfferPage } from '../lib/offer-pages';
import { SiteHeader,SiteFooter,AssessmentLink } from './site-components';
function OfferVisual({kind}:{kind:OfferPage['visual']}){
 const visual={calls:{tag:'INBOUND CALL / EXAMPLE',main:'New service enquiry',items:['Call answered','Service and location captured','Urgency checked','Team notified']},lead:{tag:'LEAD TIMELINE / EXAMPLE',main:'Website enquiry received',items:['Source identified','First reply sent','CRM updated','Follow-up scheduled']},flow:{tag:'CONNECTED WORKFLOW / EXAMPLE',main:'Form received',items:['Data validated','Record synchronized','Relevant message sent','Owner notified']},support:{tag:'SUPPORT / EXAMPLE',main:'Customer asks a question',items:['Approved answer found','Response drafted','Policy checked','Human handoff if needed']},agents:{tag:'AGENT TASK / EXAMPLE',main:'Email arrives',items:['Context retrieved','Category assigned','Action proposed','Human reviews exception']},system:{tag:'BUSINESS SYSTEM / EXAMPLE',main:'Request enters portal',items:['Access checked','Data stored','Workflow triggered','Dashboard updated']}}[kind];
 return <div className={`offer-visual offer-visual-${kind}`} aria-label={`Illustrative ${kind} workflow`}><div className="offer-visual-head"><span className="preview-dot-row"><i/><i/><i/></span>{visual.tag}<span>ILLUSTRATION</span></div><div className="offer-visual-body"><div className="offer-first"><span className="offer-first-icon">{kind==='calls'?<Phone size={22}/>:kind==='support'?<MessageSquare size={22}/>:<Workflow size={22}/>}</span><div><small>TRIGGER / 09:41</small><strong>{visual.main}</strong></div></div>{visual.items.map((item,i)=><div className="offer-step" key={item}><span className="offer-step-index">0{i+1}</span><span>{item}</span><Check size={16}/></div>)}</div><div className="offer-visual-foot">Designed around your actual process. <ArrowRight size={16}/></div></div>
}
export function OfferPageView({page}:{page:OfferPage}){return <><SiteHeader/><main><section className="wrap offer-hero"><div><p className="eyebrow">{page.eyebrow}</p><h1>{page.title}</h1><p>{page.description}</p><AssessmentLink>{page.cta} <ArrowUpRight size={18}/></AssessmentLink><small>Start with a short assessment · No obligation</small></div><OfferVisual kind={page.visual}/></section><section className="offer-problem"><div className="wrap offer-problem-inner"><span className="eyebrow">WHERE THE WORK BREAKS</span><h2>{page.problem}</h2></div></section><section className="wrap interior-section"><div className="section-intro compact"><p className="eyebrow">THE FIRST VERSION</p><h2>A clear process from <em>trigger to handoff.</em></h2></div><div className="interior-grid">{page.steps.map(([title,copy],i)=><div className="interior-card" key={title}><span className="interior-number">0{i+1} / {title.toUpperCase()}</span><h3>{title}</h3><p>{copy}</p></div>)}</div><div className="offer-examples"><div><p className="eyebrow">COMMON STARTING POINTS</p><h2>Start where the work <em>repeats.</em></h2></div><div>{page.examples.map(example=><span key={example}><Check size={17}/>{example}</span>)}</div></div><p className="sample-disclaimer">{page.note}</p><div className="interior-banner"><p className="eyebrow light">YOUR WORKFLOW, NOT A GENERIC DEMO</p><h2>Bring me the process.<br/><em>I’ll map the first version.</em></h2><p>Share where the work starts, what tools are involved and where your team gets stuck.</p><AssessmentLink light>{page.cta} <ArrowUpRight size={18}/></AssessmentLink></div></section></main><SiteFooter/></>}
