import type { Metadata } from 'next';
import { ArrowUpRight } from 'lucide-react';
import { AssessmentLink,SiteHeader,SiteFooter } from '../site-components';
export const metadata:Metadata={title:'Engagement Options',description:'Start with a free automation assessment, a scoped first workflow or a custom AI and business systems implementation.'};
const options=[['01','AI Opportunity Assessment','Free','A short review of your workflow and an initial recommendation for where to start.'],['02','First Workflow Sprint','Scoped proposal','One defined process, mapped, built, tested and documented after we agree on scope.'],['03','Custom Implementation','Scoped proposal','A larger system involving integrations, agents, dashboards, voice or custom software.']];
export default function Pricing(){return <><SiteHeader/><main className="wrap"><div className="page-hero"><p className="eyebrow">HOW TO START</p><h1>Start small. Scope the rest <em>around the work.</em></h1><p>Every process has different tools, volume and exceptions. I’ll define the deliverables and price after we understand those details.</p></div><section className="interior-section"><div className="pricing-grid">{options.map(([number,title,price,description])=><div className="pricing-card" key={title}><span>{number} / OFFER</span><h2>{title}</h2><strong>{price}</strong><p>{description}</p><AssessmentLink>{number==='01'?'Take the free assessment':'Discuss this scope'} <ArrowUpRight size={17}/></AssessmentLink></div>)}</div><p className="sample-disclaimer">No price or delivery time is promised until a workflow is scoped. A 48-hour sprint is possible only for a suitably contained project.</p></section></main><SiteFooter/></>}
